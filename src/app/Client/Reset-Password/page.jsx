import ResetPasswordContainer from "./ResetPasswordContainer";

export const metadata = {
   title: 'SCPC - Reset Password',
}

export default function ResetPasswordPage(){
   return <ResetPasswordContainer />
}